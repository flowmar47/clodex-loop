interface:
  display_name: "Clodex Loop"
  short_description: "Current agent builds; independent Claude Code review at the CLI default"
  default_prompt: "Use $clodex-loop only if this change is high-stakes and the host is not Anthropic-provided; independent reviewer at the CLI default."
